# 1.python闭包装饰器与深浅拷贝

> import copy
>
> copy.copy()	# 浅拷贝
>
> copy.deepcopy()	# 深拷贝

## 1.1闭包

### 1.1.1作用

闭包可以保存函数内的变量，而不会随着调用完函数而被销毁。

用来延长（外部）函数内变量的生命周期。

### 1.1.2闭包的语法

在函数嵌套的前提下，内部函数使用了外部函数的变量，并且外部函数返回了内部函数

这种：使用外部函数变量的内部函数称为闭包

```py
# 外部函数
def 外部函数名(外部参数):
    # 内部函数
    def 内部函数名(内部参数):
        ...[使用外部函数的变量]
    return 内部函数名	# 闭包
```

#### 闭包的构成条件：

- 有嵌套：在函数嵌套（函数里面再定义函数）的前提下；
- 有引用：内部函数使用了外部函数的变量（还包括外部函数的参数）；
- 有返回：外部函数返回了内部函数名。

> 1.函数名 和 函数名() 是两个概念，前者表示 函数对象，后者表示 调用函数，获取返回值 



## 1.2 nonloca关键字

### 1.2.1 作用

声明能够让内部函数去修改外部函数的变量

```python
'''
nonlocal关键字：
    可以使内部函数修改外部函数的变量
'''
# 1.定义外部函数
def func_outer():
    # 2.定义外部函数的（局部）变量
    a = 100
    # 3.定义内部函数
    def func_inner():
        # 4.使用 nonlocal 关键字声明外部函数的变量，可以使内部函数修改外部函数的变量
        nonlocal a
        a = a + 1
        print(f'a is {a}')
    # 5.返回内部函数名
    return func_inner

# 测试
if __name__ == '__main__':
    fn_inner = func_outer()
    fn_inner()  # 101
    fn_inner()  # 102
    fn_inner()  # 103
```

在执行 fn_inner = func_outer() 语句后，func_outer()从内存中释放，只留下函数 fn_inner() 即函数 func_inner()，故属于函数 func_outer() 的局部变量 a 就从内存中释放。

因此，在内部函数中只能调用外部函数的局部变量，却无法对其进行修改。

只能使用 nonlocal 关键字来声明局部变量，延长其生命周期。

![Snipaste_2026-01-29_21-51-00](D:\Study\notes\python\assets\Snipaste_2026-01-29_21-51-00.png)

## 1.3装饰器

### 1.3.1装饰器的作用

不改变原有函数的基础上，给原有函数增加额外的功能。

装饰器本质上就是一个闭包函数。

### 1.3.2构成条件

- 有嵌套：在函数嵌套（函数里面再定义函数）的前提下；
- 有引用：内部函数使用了外部函数的变量（还包括外部函数的参数）；
- 有返回：外部函数返回了内部函数名（函数对象）；
- 有额外功能：给需要装饰的原有函数增加额外功能。

### 1.3.3用法

- 传统写法

```python
装饰后的函数名 = 装饰器名(被装饰的原函数名)
装饰后的函数名()
```

- 语法糖（简化用法）

```python
# 在要被装饰的原函数上，直接写@装饰器名，之后直接调用即可
@装饰器名
```

### 1.3.4装饰器的使用

- 无参无返回值的函数

定义：def 函数名(): ...

调用：函数名()

- 有参无返回值的函数

定义：def 函数名(形参): ...

调用：函数名(实参)

- 无参有返回值的函数

定义：def 函数名(): ... 	return 返回值

调用：用变量接收返回值 = 函数名()

- 有参有返回值的函数

定义：def 函数名(形参): ...	return 返回值

调用：用变量接收返回值 = 函数名(实参)



### 1.3.5多个装饰器装饰一个函数

多个装饰器装饰1个函数，是按照由内向外的顺序来装饰的，但如果你要是用装饰器的写法来做，看到的效果是：从上往下执行的



### 1.3.6带有参数的装饰器

一个装饰器的参数有且只能有一个。

如果装饰器有多个参数，可以在该装饰器的外边再包裹一层，把该装饰器当作其内部函数即可。

使用带有参数的装饰器，其实是在装饰器外面又包裹了一个函数，使用该函数接收参数，返回装饰器。

## 1.4深浅拷贝

### 1.4.1可变类型与不可变类型

不可变对象：

一旦创建就不可修改的对象，包括字符串、元组、数值类型（整型、浮点型）

该对象所指向的内存中的值不能被改变。当改变某个变量时，由于其所指的值不能被改变，相当于把原来的值复制一份后再改变，这会开辟一个新的地址，变量再指向这个新的地址。

可变对象：

可以修改的对象，包括列表、字典、集合

该对象所指向的内存中的值可以被改变。变量（准确的说是引用）改变后，实际上是其所指的值直接发生改变，并没有发生复制行为，也没有开辟新的地址，通俗点说就是原地改变。

### 1.4.2变量赋值执行原理

```python
a = "python"
```

python解释器干的事情：

①创建变量a

②创建一个对象（分配一块内存），来存储值'python'

③将变量与对象，通过指针连接起来，从变量到对象的连接称之为引用（变量引用对象）

![Snipaste_2026-02-01_11-31-17](D:\Study\notes\python\assets\Snipaste_2026-02-01_11-31-17.png)

### 1.4.3浅拷贝概念与原始数据准备

浅拷贝：创建新对象，其内容是原对象的引用。

浅拷贝 之所以称为浅拷贝，是它仅仅只拷贝了一层，拷贝了最外围的对象本身，内部的元素都只是拷贝了一个引用而已。

原始数据：

![Snipaste_2026-02-01_11-33-48](D:\Study\notes\python\assets\Snipaste_2026-02-01_11-33-48.png)

### 1.4.4可变类型浅拷贝和不可变类型浅拷贝

- 可变类型浅拷贝

![Snipaste_2026-02-01_11-37-16](D:\Study\notes\python\assets\Snipaste_2026-02-01_11-37-16.png)

- 不可变类型浅拷贝

![Snipaste_2026-02-01_11-40-12](D:\Study\notes\python\assets\Snipaste_2026-02-01_11-40-12.png)

### 1.4.5可变类型深拷贝和不可变类型深拷贝

- 可变类型深拷贝

拷贝所有的（可变的）层

![Snipaste_2026-02-01_13-52-41](D:\Study\notes\python\assets\Snipaste_2026-02-01_13-52-41.png)

- 不可变类型深拷贝

深拷贝不可变类型：若为不可变类型直接就引用了，不开辟新的空间



# 2.网编和多线程

网编=用来实现不同计算机上运行的程序间进行数据交互

IP，端口号，协议

进程，线程都是程序（CPU）实现多任务的手段	

进程=CPU分配资源的最小单位

线程=CPU调度资源的基本单位

进程间数据隔离，线程间数据共享

## 2.1网络编程

网络编程三要素：IP地址，端口，协议

> 1.知道IP地址的作用
>
> 2.知道端口和端口号的作用
>
> 3.能够说出TCP的特点

网络：将具有独立功能的多台计算机通过通信线路和通信设备连接起来，在网络管理软件及网络通信协议下，实现资源共享和信息传递的虚拟平台。

能够编写基于网络通信的软件或程序，通常来说就是网络编程。

### 2.1.1基本概念

要使用编程语言实现多台计算机的网络通信，需要具备网络编程三个要素：

（1）IP地址：这是网络环境下每一台计算机的唯一标识，通过IP地址来找到指定的计算机；

（2）端口：用于标识进程的逻辑地址，通过端口来找到指定的进程；

（3）协议：定义通信规则，符合协议则可以通信，否则无法正常通信。

例如：假如我要和小明面对面说上话。

- 首先我要到【小明的住处】找到小明（相当于通过IP地址找到指定计算机）；
- 然后我要和小明说话，小明使用【耳朵听】我说（相当于用端口接收）；
- 最后，我们对话不能随便使用任意语言，此时说的话【彼此都能听懂】，如讲普通话。

#### IP地址

查看IP地址与检查网络

Linux和mac OS使用ifconfig命令

Windows使用ipconfig命令



检查网络是否正常使用ping命令

- ping www.baidu.com 检查是否能上公网
- ping 当前局域网的IP地址 检查是否在同一个局域网内
- ping 127.0.0.1 检查本地网卡是否正常

#### 端口和端口号

端口号可以标识电脑中唯一的一个端口

知名端口号：是指众所周知的端口号，范围从0到1023

动态端口号：一般程序员开发应用程序使用端口号称为动态端口号，范围是从1024到65535

#### 协议

> 传输协议，规范
>
> 常用的协议：
>
> ​	TCP（打电话）（这个用的最多）和UDP（群聊）
>
> TCP特点：
>
> ​	1.面向有连接
>
> ​	2.采用字节流传输数据，理论无大小限制
>
> ​	3.安全（可靠）协议
>
> ​	4.效率相对（UDP）较低
>
> ​	5.区分客户端和服务器端

### 2.1.2TCP协议

#### 通信流程

通过IP地址能够找到对应的设备，然后再通过端口号找到对应的端口，再通过端口把数据传输给应用程序。要注意，数据不能随便发送，在发送之前还需要规则，以保证程序之间按照指定的规则来进行数据的通信，而这个规则就是TCP协议。

#### TCP的概念

TCP（Transmission Control Protocol）简称传输控制协议，它是一种面向连接的、可靠的、基于字节流的传输层通信协议。

#### TCP面向连接

TCP通信步骤：

- 创建链接
- 传输数据
- 关闭连接

说明：

TCP通信模型相当于生活中的“打电话”，在通信开始之前，一定要先建立好连接，才能发送数据，通信结束要关闭连接

#### TCP协议创建连接：3次握手

三次握手（Three-Way Handshake）就是指建立一个TCP连接时，需要客户端和服务器端总共发送3个包以确认连接的建立

- 第一次握手：客户端向服务器端发送请求，等待服务器端确认。
- 第二次握手：服务器端收到请求后知道客户端请求建立连接，回复给客户端以确认连接请求。
- 第三次握手：客户端收到确认后，再次发送请求确认给服务器端，服务器端收到正确请求后，如果正确则连接建立成功，完成三次握手，随后客户端与服务器端之间可以开始传输数据了。

![Snipaste_2026-02-01_19-30-37](D:\Study\notes\python\assets\Snipaste_2026-02-01_19-30-37.png)

#### TCP协议断开连接：4次挥手

四次挥手说TCP断开连接的时候需要经过4次确认。TCP连接是双向，A连接B、B连接A都要断开。

- 第一次挥手：当主机A（可以是客户端也可以是服务器端）完成数据传输后，提出停止TCP连接的请求
- 第二次挥手：主机B收到请求后对其作出响应，确认这一方向上的TCP连接将关闭
- 第三次挥手：主机B再提出反方向的连接关闭请求
- 第四次挥手：主机A对主机B的请求进行确认，双方向的关闭结束

### 2.1.3Socket套接字与TCP开发流程

#### Socket套接字

概念

> Socket（简称 套接字）是进程之间通信的一个工具，好比打电话时，通话的不是客户端与服务器端，而是客户端与服务器端通过手机这个工具来进行通话。进程之间想要进行网络通信需要基于这个Socket

使用场景

> 负责进程之间的网络数据传输，好比数据的搬运工。
>
> 只要跟网络相关的应用程序或者软件都使用到了socket

使用Socket

```py
# 导入socket模块
import socket
```

> 要使用socket，则通常要使用到socket模块下的socket类创建socket对象：
>
> socket(AddressFamily, Type) 	用于TCP协议

### 2.1.4TCP开发流程

#### TCP网络应用程序开发分为：

- TCP客户端程序开发（ps：浏览器）
- TCP服务器端程序开发

说明：

> 客户端程序是指运行在用户设备上的程序
>
> 服务器端是指运行在服务器设备上的程序，专门为客户端提供数据服务。

TCP服务器端

①创建服务器端套接字对象

②绑定IP地址和端口号

③设置最大监听数

④等待客户端连接服务器端

⑤给客户端发送信息

⑥接收客户端消息并打印

⑦关闭连接释放资源

TCP客户端

①创建客户端套接字对象

②客户端连接服务器端

③接收数据

④发送数据

⑤关闭套接字

![Snipaste_2026-02-01_23-08-04](D:\Study\notes\python\assets\Snipaste_2026-02-01_23-08-04.png)

#### 设置端口号复用

让服务器端程序退出后端口号立即释放

```python
# 参数1：表示当前套接字
# 参数2：设置端口号复用选项
# 参数3：设置端口号复用选项对应的值
tcp_server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, True)
```

## 2.2进程和线程

### 2.2.1多任务执行

概述：

> 让多个任务“同时”执行，目的是：充分利用CPU资源，提高程序的执行效率。

- 方式
  - 并发：针对单核CPU来讲，多个任务同时请求执行时，CPU做高效切换即可。
  - 并行：针对多核CPU来讲，多个任务同时执行。

### 2.2.2进程

进程的概念：进程是CPU资源分配的最小单位，它是操作系统进行资源分配和调度运行的基本单位

通俗理解：一个正在运行的程序就是一个进程。例如：正在运行的qq，微信等他们都是一个进程

一个程序运行后至少有一个进程。

python中多进程的基本工作方式：程序运行起来主进程，在主进程上创建子进程

#### 进程的创建步骤

```python
# 1.导入进程工具包
import multiprocessing
# 2.通过进程类实例化进程对象
子进程对象 = multiprocessing.Process()
# 3.启动进程执行任务
进程对象.start()

# 通过进程类实例化创建进程对象
子进程对象 = multiprocessing.Process(group = None, target = None, name = None, args = (), kwargs = ())
```

group：参数未使用，值始终为None

target：表示调用对象，即子进程要执行的任务（回调函数入口地址）

args：表示以元组的形式向子任务函数传参，元组方式传参一定要和参数的顺序保持一致

kwargs：表示以字典的方式给子任务函数传参，字典方式传参字典中的key要和参数名保持一致

name：子进程的名称

#### 进程传参

- args：位置传参
- kwargs：字典传参

#### 进程编号

进程编号唯一标识一个进程，方便管理进程。

在一个操作系统中，一个进程拥有的进程号是唯一的，进程号可以反复使用。

获取进程编号的目的是验证主进程和子进程的关系，可以得知子进程是由哪个主进程创建出来的。

获取进程编号的两种操作：

- 获取当前进程编号

```py
import os
# 获取当前进程编号
pid = os.getpid()
print(pid)
# ----
import multiprocessing
pid = multiprocessing.current_process().pid
print(pid)
```

- 获取当前父进程编号

```py
# 获取父进程的编号
ppid = os.getppid()
print(ppid)
```

#### 进程注意点

- 进程之间不共享全局变量

因为子进程相当于是父进程的“副本”，会将父进程的“main外资源”拷贝一份，即：各是各的

![Snipaste_2026-02-04_22-40-32](D:\Study\notes\python\assets\Snipaste_2026-02-04_22-40-32.png)

- 主进程会等待所有的子进程执行结束后再结束

不让主进程等待子进程：

方法一：子进程设置守护进程

子进程设置守护主进程的目的：是主进程退出子进程销毁，不让主进程再等待子进程

方法二：子进程自己主动的终止子进程（不建议使用，不会清理资源）

### 2.2.3线程

概念：

> 进程是分配资源的基本单位，一旦创建一个进程就会分配一定的资源
>
> 线程是CPU调度的基本单位，每个进程至少有一个线程，而这个线程就是我们常说的主线程

线程创建的步骤：

```python
# 1.导入线程模块
import threading
# 2.通过线程类创建线程对象
线程对象 = threading.Thread(target = 任务名)
# 3.启动线程执行任务
线程对象.start()
```

#### 线程的注意点介绍

1.线程之间执行是无序的

2.主线程会等待所有的子线程结束后再结束

3.线程之间共享全局变量

4.线程之间共享全局变量数据出现错误问题

> 解决方案：
>
> 线程同步：保证同一时刻只能有一个线程去操作全局变量（加锁）

```py
# 互斥锁的使用流程
# 1.互斥锁的创建
mutex = threading.Lock()
# 2.上锁
mutex.acquire()
# 3.释放锁
mutex.release()
```

#### 死锁

一直等待对方释放锁的情景就是死锁。

死锁的原因：使用互斥锁的时候需要注意死锁的问题，未在合适的地方注意释放锁

死锁的结果：会造成应用程序的停止响应，应用程序无法再继续往下执行了

### 2.2.4区别对比

1.进程之间不共享全局变量

2.线程之间共享全局变量，但是要注意资源竞争的问题，解决办法：互斥锁

3.创建进程的资源开销要比创建线程的资源开销大

4.进程是操作系统资源分配的基本单位，线程是CPU调度的基本单位

5.线程不能够独立执行，必须依存在进程中执行

6.python中多进程开发比单进程多线程开发稳定性要强

#### 优缺点

进程：

- 优点：可以用多核
- 缺点：资源开销大

线程：

- 优点：资源开销小
- 缺点：不能使用多核

# 3.迭代器、生成器与正则表达式

## 3.1迭代器

迭代器（Iterator）是python中的一种对象，用于在数据集合中逐个访问元素，而不需要暴露数据集合的底层实现。它提供了一种遍历集合元素的标准方式，适用于任何支持迭代的数据结构，如列表、元组等，range()就是一个迭代器

迭代器是一个实现了`__iter__()`he`__next__()`方法的对象，使得可以逐步遍历它的元素

特点：

手动管理：需要显示地实现`__iter__()`he`__next__()`方法

状态管理：迭代器需要自己管理迭代的状态，包括当前位置和结束条件

内存使用：内存使用取决于迭代器的实现，通常是惰性计算（即按需生成数据）

## 3.2生成器

概念：

> 根据程序员指定的规则循环生成数据，当条件不成立时则生成数据结束。数据不是一次性全部生成出来，而是使用一个，再生成一个，可以节约内存。

创建生成器的方式：

- 生成器推导式
- yield关键字

## 3.3property属性

>  把函数当属性用，简化代码属性

- 装饰器用法

get方法使用@property装饰，set方法使用@获取函数名.setter来装饰，两个函数使用同一函数名即可

- 类属性用法

将get和set方法封装到类属性中，类属性名 = property(get, set)

## 3.4正则表达式

