# 核心
提出：<B>用一个MLP神经网络来表示一个连续的三维场景，把空间位置和观察方向输入网络，输出该点的体密度和颜色，再通过可微体渲染合成任意新视角图像。</b>

NeRF不是直接重建网格、点云或体素，而是把一个场景“存进神经网络参数里”。
论文将场景表示为一个连续的<b>5D神经辐射场</b>：
输入：照片和相机位姿——三维位置（x,y,z）和二维观察方向（θ,ϕ）
输出：新视角的渲染图——体密度 σ 和与视角相关的 RGB 颜色



